package com.semgt.service;

import java.util.List;
import java.util.Map;

import com.semgt.model.Rule;
import com.semgt.model.RuleKey;

public interface IRuleService {
	List<Rule> qryAll();

//	List qryRule(RuleKey ruleKey);
}
